const DATA_ERROR = {
    JSON_LIST_INVALID:
        'List contains invalid json and not following structure {key : <any>, value : <any>}',
    SERVICE_NOT_SUPPORTED: 'Currently not supported'
};

export default DATA_ERROR;
